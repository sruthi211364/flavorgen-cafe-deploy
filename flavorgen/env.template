# .env  —  copy this file, fill in your values, never commit it to git
# The actual .env file is listed in .gitignore

# ── MongoDB Atlas ─────────────────────────────────────────────
# Replace <db_password> with your actual Atlas password
MONGODB_URI=mongodb+srv://flavorgen_user:<db_password>@flavorgen-cafe.6c8nqke.mongodb.net/?appName=flavorgen-cafe

# ── App settings ─────────────────────────────────────────────
# Set to "true" once you have run migrate_to_mongo.py
LOAD_FROM_MONGO=true
